<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/style.css" >
    <title>Song Reviews</title>
  </head>
  <body>

    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="/songreview">Karaoke-Box Songreview</a>
        </div> <!-- closes navbar header-->
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <% if (!loggedinUser){ %>
              <li><a href="/songreview/login">Login</a></li>
              <li><a href="/songreview/register">Sign Up</a></li>
            <% } else { %>
              <li> <a href="#">Hello <%= loggedinUser.username %></a> </li>
              <li><a href="/songreview/logout">Logout</a></li>
            <% } %>
          </ul>
        </div> <!-- closes navbar collapse -->
      </div><!--close container fluid -->
    </nav>

<div class="container">
  <% if (error && error.length > 0) { %>
  <div class="alert alert-danger" role="alert"><%= error %></div>
  <% } %>
  <% if (success && success.length > 0) { %>
  <div class="alert alert-success" role="alert"><%= success %></div>
  <% } %>
</div>
